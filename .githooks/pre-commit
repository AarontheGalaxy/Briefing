#!/bin/sh
# Block the commit if gitleaks finds a secret in the staged changes.
if ! command -v gitleaks >/dev/null 2>&1; then
    echo "pre-commit: gitleaks not installed (brew install gitleaks). Commit blocked." >&2
    exit 1
fi
exec gitleaks git --pre-commit --staged --redact -v
